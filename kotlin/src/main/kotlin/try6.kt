fun imprimir(s: String): Unit {
    println(s)
}

fun soma(a: Int, b: Int): Int = a + b

fun main() {
    // por padrão, uma função em kotlin retorna "Unit", que seria como o "Void" do java
    // a função sem retorno pode ou não especificar que vai retornar Unit

    // também é possível utilizar de Single-Expression functions, como funções lambda.
}